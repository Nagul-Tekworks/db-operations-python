# db-operations-python


# 📊 Data Loading with Python

This repository demonstrates how to connect **Python** with different data sources — **MySQL**, **Supabase**, and **REST APIs** — and load data into **Pandas DataFrames**.

### Files Included
- `mysql_connectivity.py` → Load data from MySQL  
- `supabase_connectivity.py` → Load data from Supabase  
- `api_data_loader.py` → Load data from REST API  

### Requirements
```bash
pip install pandas mysql-connector-python psycopg2-binary requests

